# 🚀 ШВИДКИЙ СТАРТ

## ✅ Виконайте ЦІ 3 КОМАНДИ:

### Windows:
```cmd
cd C:\Users\user\IdeaProjects\lab-system-full
git pull
start.bat
```

### Linux/Mac:
```bash
cd ~/lab-system-full
git pull
./start.sh
```

## 🌐 Відкрийте браузер:

http://localhost:3000

## 👤 Увійдіть:

**Логін:** `admin`
**Пароль:** `admin123`

---

## ❓ Якщо щось не працює:

```cmd
docker compose down -v
docker compose up -d --build
```

Зачекайте 30 секунд і спробуйте знову.

## 📝 Перегляд логів:

```cmd
docker compose logs -f backend
docker compose logs -f frontend
```

---

**ВСЕ! Більше нічого не потрібно!**
